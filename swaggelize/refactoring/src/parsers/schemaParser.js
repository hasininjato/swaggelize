function parseInput(collections) {
    // console.log(JSON.stringify(collections, null, 4));
    Object.keys(collections).forEach(function (key) {
        // console.log(collections[key]);
    })
}

module.exports = parseInput;